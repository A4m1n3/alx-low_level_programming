#include "main.h"

/**
 * print_last_digit - check the code.
 *
 * Return: n look at code again.
 * @n: is the input we want to check
 */
int print_last_digit(int n)
{
	int tmp = n % 10 ;
	if tmp >=0
		return (tmp);	
	else 
		return (-tmp);
}
