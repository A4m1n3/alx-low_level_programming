#include "main.h"

/**
 * _abs - check the code.
 *
 * Return: 1 if >0 ; 0 otherwise.
 * @n: is the input we want to check
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	else
	{
		return (-n);
	}

}
